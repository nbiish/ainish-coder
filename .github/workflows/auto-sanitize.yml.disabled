name: Auto-Sanitize Secrets

on:
  push:
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  sanitize:
    uses: ./.github/workflows/reusable-sanitize.yml
    with:
      target-paths: '.'
