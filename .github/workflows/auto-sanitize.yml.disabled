name: Auto-Sanitize Secrets

on:
  push:
    branches: [ main, dev, staging ]
    paths:
      - 'CONFIGURATIONS/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'CONFIGURATIONS/**'

permissions:
  contents: write
  pull-requests: write

jobs:
  sanitize:
    uses: ./.github/workflows/reusable-sanitize.yml
    with:
      target-paths: 'CONFIGURATIONS/**'
