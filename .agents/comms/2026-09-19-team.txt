---
description: Agent Communication System protocol template. Deployed by `ainish-coder --rules` as `.agents/comms/{date}-{time}-team.txt` (one rotating file per team session, UTC-stamped at creation). Merge-safe: agents deploying updates must preserve all entry blocks below the PROTOCOL section; only the PROTOCOL block itself may be refreshed from this template.
---

# AGENT COMMS — 2026-09-19

<PROTOCOL>
## PROTOCOL (binding — follow like AGENTS.md)

**When:** ≥1 agent or subagent is working at once (multiple branches, features, updates, bugs, docs). Solo? Still check in/out — the ledger is the audit trail.

**Two channels:**
- **This file** (`.agents/comms/{date}-{time}-team.txt`) — committed, append-only durable ledger. Updated via your task-branch commits; reaches everyone at merge.
- **Live board** (`.agents/comms/{date}-team.live.txt`) — gitignored, at the **main repo path**. Append here by absolute path for immediate in-flight visibility. Never `git commit` it; never `git add` it. Main stays clean.

**Read:** this file + the live board — at start, before any merge, and whenever blocked.

**Write:** for every lifecycle event, append one entry block to **both** the live board (immediate) and this file in your worktree (durable — commit it). One writer per branch. Subagents do not write the shared files directly: report results to your orchestrator, who logs on your behalf with `parent:` set.

**Skill distribution (persisted per-repo selection):** which `.agents/skills/` packs distribute to this repo is governed by the operator's persisted selection (`~/.config/ainish-coder/skills-selection.json`, toggled via `ainish-coder --skills-sync` or `--rules`). Agents and CI MUST use the headless path — `ainish-coder --skills-sync <dir> --headless` or `ainish-coder --rules <dir> --headless` — which applies the saved selection with no prompts. Do not hand-edit the selection file; run the interactive toggle instead. Scroll-channel packs (`8thfire-scrolls`, `ghost-layer-injector`, raw `.scrolls*/` payload) are never selectable here — explicit `--scrolls` only.

**Timestamps — mandatory, bracketed, ISO-8601 (UTC `Z` form on this ledger):**
- `start:` the instant the agent **begins** the action (input received / work begun).
- `end:` the instant the agent **finishes** it (output produced / work handed off or halted).
- **Every** entry has both. Abandoned mid-action → still close with `end:` + `status: abandoned`.

**Entry block:**

~~~
### [<ISO-8601 timestamp>] <EVENT> | agent:<name> | branch:<branch> | wt:<worktree-path>
- start:<ISO-8601>               # input received / work began
- end:<ISO-8601>                 # output produced / work ended
- parent:<orchestrator>          # subagents only
- scope:<paths/modules>
- objective:<one line>
- input:<one line — what was received or requested>
- output:<one line — what was produced or handed off>
- status:<new|in-progress|blocked|verifying|merged|abandoned>
- blockers:<none | description>
~~~

</PROTOCOL>

---

### [2026-09-19T04:38:00Z] checkin | agent:antigravity | branch:chore/agents-suggestions-schema | wt:d:/Code/ainish-coder-suggestions
- start:2026-09-19T04:38:00Z
- end:2026-09-19T04:38:00Z
- scope:AGENTS.md,llms.txt,bin/git-context.sh,bin/lib/auto_thinking_context.sh,bin/ainish-coder,src/deploy_skills.sh,src/verify_skills.sh,.agents/tasks/2026-09-19-task.md,.agents/suggestions/2026-09-19-suggestions.md
- triage:now
- objective:refactor AGENTS.md instructions to record masters' suggestions in .agents/suggestions/{date}-suggestions.md and review suggestions instead of TTS, maintaining masters schema, and refactor task schema to .agents/tasks/{date}-task.md
- input:user request
- output:task active
- status:active
- blockers:none

### [2026-09-19T04:42:00Z] intent-merge | agent:antigravity | branch:chore/agents-suggestions-schema | wt:d:/Code/ainish-coder-suggestions
- start:2026-09-19T04:42:00Z
- end:2026-09-19T04:42:00Z
- scope:AGENTS.md,llms.txt,bin/git-context.sh,bin/lib/auto_thinking_context.sh,bin/ainish-coder,src/deploy_skills.sh,src/verify_skills.sh,.agents/tasks/2026-09-19-task.md,.agents/suggestions/2026-09-19-suggestions.md
- objective:merge chore/agents-suggestions-schema -> main — refactor masters' suggestions into .agents/suggestions/{date}-suggestions.md without TTS and align task schema to .agents/tasks/{date}-task.md
- input:user directive; green gates (security_gate clean, bash -n clean)
- output:merging now; no-rebuild-needed (scripts/docs only)
- status:merged
- blockers:none
