# Task: Refactor AGENTS.md Masters' Suggestions and Task Schema

- Date: 2026-09-19
- Branch: chore/agents-suggestions-schema
- Status: complete
- Scope: AGENTS.md, llms.txt, bin/git-context.sh, bin/lib/auto_thinking_context.sh, bin/ainish-coder, src/deploy_skills.sh, src/verify_skills.sh, .agents/tasks/2026-09-19-task.md, .agents/suggestions/2026-09-19-suggestions.md
- Triage: now

## Objective
Refactor `AGENTS.md` instructions to have agents record masters' suggestions in `.agents/suggestions/{date}-suggestions.md` and review suggestions instead of using TTS, while maintaining the 9-master schema. Refactor the task file specification to `.agents/tasks/{date}-task.md` to maintain consistency across `.agents/` folders (`comms/{date}-team.txt`, `tasks/{date}-task.md`, `suggestions/{date}-suggestions.md`).

## Changes Completed
1. `AGENTS.md`:
   - Replaced `<OUTPUT>` section (`END-OF-CHAT VOICE SUMMARY`) with `END-OF-CHAT MASTERS' SUGGESTIONS & REVIEW (MANDATORY)` instructing agents to review suggestions in `.agents/suggestions/{date}-suggestions.md` and record the masters' suggestions per turn.
   - Preserved the full 9-master schema along with the fused summary and `Next step:` imperative.
   - Removed `cli-tts` references from frontmatter, division of labor (§DOCUMENT_MODEL), and `<REINFORCEMENT>` block.
   - Refactored task schema across `AGENTS.md` from `TASK.{date}.{slug}.md` to `.agents/tasks/{date}-task.md` (with optional `{date}.{slug}-task.md` for concurrency).
   - Added `.agents/suggestions/{date}-suggestions.md` and `.agents/tasks/{date}-task.md` to `<COMMS>` coordination specifications, carve-outs, and remote record requirements.
2. `llms.txt`:
   - Removed `cli-tts` reference from custom tooling list.
   - Updated documentation boundary notes to include `.agents/suggestions/` alongside `.agents/tasks/`.
3. Tooling & Scripts:
   - `bin/git-context.sh`: Updated task discovery to scan `*-task.md` alongside legacy `TASK.*.md`, and added a new `SUGGESTIONS` overview section displaying recent entries from `.agents/suggestions/*-suggestions.md`.
   - `bin/lib/auto_thinking_context.sh`: Updated slug discovery to support `*-task.md` format.
   - `bin/ainish-coder`, `src/deploy_skills.sh`, `src/verify_skills.sh`: Added scaffolding for `.agents/suggestions/` alongside `.agents/tasks/`.
4. Verification:
   - Ran `py -3 bin/security_gate.py`: PASSED (zero banned crypto or unverified open paths detected).
   - Ran `bash -n` on all modified bash scripts: PASSED (zero syntax errors).
   - Created initial `.agents/suggestions/2026-09-19-suggestions.md` and recorded initial masters' suggestions.
