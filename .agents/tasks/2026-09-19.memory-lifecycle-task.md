# Task: Agent Memory Lifecycle Integration (Make / Update / Delete)

- Date: 2026-09-19
- Branch: feat/agent-memory-lifecycle
- Status: complete
- Scope: AGENTS.md, .agents/canonical/, .agents/skills/repo-memory-graph-intelligence/SKILL.md, .agents/skills/repo-memory-graph-intelligence/references/memory-crud-matrix.md, .agents/memories/llms.txt, .agents/memories/MEMORY.md, bin/git-context.sh, src/deploy_skills.sh, src/verify_skills.sh, llms.txt
- Triage: now

## Objective
Update `AGENTS.md`, `.agents/skills/repo-memory-graph-intelligence/`, and `.agents/memories/` to establish an active, deterministic memory CRUD lifecycle (Make, Update, Delete/Resolve) integrated across agent chat sessions, task workflows, and git tracking.

## Changes Completed
1. `AGENTS.md`:
   - Updated frontmatter and `<TASK_PRIMER>` to include active in-flight memory CRUD and fast memory orientation.
   - Refactored `<WORKFLOW>` steps (Coordinate, Recon, Iterate, Verify, Merge, Cleanup) with explicit memory checkpoints.
   - Expanded `<REPO_MEMORY>` to define the Three-Tier Integration Model (Chat Session Lifecycle, Task Lifecycle, and Continuous CRUD Protocol).
   - Mandated active invalidation (retiring dead facts and moving resolved gotchas to archive) to prevent zombie/contradictory knowledge.
   - Added Pre-Commit Checklist item 10 for Memory Lifecycle & CRUD verification.
   - Re-snapshotted canonical contract in `.agents/canonical/` with updated sha256.
2. `.agents/skills/repo-memory-graph-intelligence/`:
   - Updated frontmatter description to reflect active CRUD across chat sessions and tasks.
   - Upgraded Section 5 with the Continuous Session & Task Memory Lifecycle Protocol (Phase A Orient, Phase B Act/Mutate, Phase C Consolidate).
   - Upgraded Section 6 checklist with in-flight memory mutation (Make, Update, Delete/Resolve).
   - Added Section 7: "The Memory CRUD Lifecycle Engine: Chat Session & Task Integration" detailing exact triggers, schemas, and commands for Make, Update, Delete/Resolve, and turn-by-turn playbooks.
   - Created `references/memory-crud-matrix.md` with full tool schemas, parameter examples, CLI fallbacks, and markdown templates.
3. `.agents/memories/`:
   - Updated `.agents/memories/llms.txt` contract with lifecycle states (`[ACTIVE]`, `[UPDATED]`, `[RESOLVED]`), canonical syntax, and structured section layout.
   - Restructured `.agents/memories/MEMORY.md` into Active Facts, Active Decisions, Active Gotchas, and Resolved/Retired Archive.
   - Tagged existing entries with source, task, and AST references; added new entries for the memory CRUD lifecycle and git-context integration.
4. Tooling & Scaffolding:
   - `bin/git-context.sh`: Added `--- MEMORY ---` section outputting active memory metrics and top recent active invariants for instant agent situational awareness.
   - `src/deploy_skills.sh` & `src/verify_skills.sh`: Added scaffolding for `$target_dir/.agents/memories` and `.agents/memories/exports`.
   - `llms.txt`: Updated memory record layer descriptions and Child DOX index.
5. Verification:
   - Validated bash syntax across modified scripts (`bash -n`).
   - Verified `bin/git-context.sh` output under Git Bash.
   - Verified Zero-Trust gate (`py -3 bin/security_gate.py`): PASSED (0 violations).
