# TASK.2026-09-01.agents-deployed-strip-ainish

**Branch:** `refactor/agents-strip-ainish-coder` (worktree `../ainish-coder-agents-strip`, from `main`)

## Objective
1. Strip all `ainish-coder` specific mentions from `AGENTS.deployed.md`, `src/templates/AGENTS.deployed.md`, and `src/templates/AGENTS.md`.
2. Ensure `AGENTS.deployed.md` functions as a pure, directional system prompt file for any AI pair-programming model across any repository, focusing strictly on:
   - Communication protocols (`AGENTS/{date}.COMMS.md`, `wtf` hub)
   - Documentation & PRD standards (`llms.txt`, `.agents/tasks/`)
   - Worktree isolation & git workflow
   - PQC secrets management & cryptographic compliance
   - Skills integration & modular capability best practices (consulting skills, byte-identity, safe synchronization, project-specific foreign skill preservation)
   - Polyglot code quality, verification, and zero-trust security gates
3. Verify all security checks (FIPS 203/204/205 compliance, no banned algorithms or secrets).

## Verification
- Search for `ainish` across `AGENTS.deployed.md`, `src/templates/AGENTS.deployed.md`, and `src/templates/AGENTS.md` to ensure zero occurrences.
- Confirm structural integrity of all sections, workflows, and protocols in deployed rules files.
- Run security scan / audit checklist.
