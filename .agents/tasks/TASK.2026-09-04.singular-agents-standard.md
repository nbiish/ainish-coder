# TASK.2026-09-04 — singular AGENTS.md standard

Operator directive: one AGENTS.md used here AND deployed everywhere.
Root AGENTS.md verified already repo-agnostic; added DOCUMENT_MODEL block.
llms.txt crowned per-repo PRD in both boundary + conflict-order rules.
Retired AGENTS.deployed.md + src/templates/AGENTS.deployed.md (stale pre-v2).
deploy_agents.sh: sources root AGENTS.md; drops obsolete checkout guard.
Keeps legacy-cleanup of old dual-contract files at targets.
bin/ainish-coder + help.sh: strings updated to singular model.
src/templates/llms.txt: Core Contract teaches the model to targets.
llms.txt: stale pack count fixed 21 -> 23.
Smoke: --agents to seeded tmp target -> byte-identical AGENTS.md,
legacy files cleaned, self-deploy reports same-file. All PASS.
Post-merge: re-run deploy_agents_global from main (symlinks -> root file).
DOX pass: boundary + hierarchy rows updated; no other contracts touched.

####
Ship singular-contract model; merge; repoint global symlinks post-merge.
