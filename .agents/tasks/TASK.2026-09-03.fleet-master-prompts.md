# TASK.2026-09-03.fleet-master-prompts

**Branch:** `feat/agents-fleet-master-prompts` (worktree `../ainish-coder-fleet-prompts`, from `main`)

## Operator Contract
1. Deeply integrate canonical master prompt templates (`TPL_TRAE_AST_V2`, `TPL_MINI_TDD_REPRO_V1`, `TPL_SECURITY_AUDIT_V1`, `TPL_TRAE_SYSTEMS_V1`) into `AGENTS.md`, `AGENTS.deployed.md`, and `src/templates/AGENTS.deployed.md`.
2. Fully harmonize prompt specifications with `.agents/skills/trae-mini-fleet/SKILL.md`.
3. Incorporate the latest text-to-speech master directives from `AGENTS-TTS-COMMS.txt`:
   - Enforce 100% byte identity between `AGENTS.md`, `AGENTS.deployed.md`, and `src/templates/AGENTS.deployed.md`.
   - Re-anchor loopback 11434 single-config proxy boundaries.
   - Pinned supply chain coordinates and verified binary tool paths.
   - Zero-config headless tool call directives.
   - Argument discovery via `--help`.
