# Task: Deploy AGENTS.md as Protected Symlink from Root Repo

- Date: 2026-09-19
- Branch: feat/rules-symlink-protection
- Status: complete
- Scope: AGENTS.md, llms.txt, bin/ainish-coder, bin/security_gate.py, src/deploy_agents.sh, src/agents_protection.sh, src/help.sh, .agents/canonical/
- Triage: now

## Objective
Deploy `AGENTS.md` deployed with `ainish-coder --rules` (and `--agents`) as a live symbolic link pointing to the root `ainish-coder` repository (`${REPO_DIR}/AGENTS.md`), ensuring that editing it at root updates all codebases at once, while protecting the root `AGENTS.md` from being overwritten or corrupted by agents or scripts operating in receiving repositories.

## Changes Completed
1. `src/agents_protection.sh`:
   - `lock_agents_contract`: Sets OS-level write protection (`attrib +R` on Windows, `chmod 444` on POSIX) on root `AGENTS.md`.
   - `unlock_agents_contract`: Unlocks `AGENTS.md` for authorized edits in the root repository.
   - `is_agents_contract_locked`: Audits file write protection state.
   - `snapshot_canonical_contract`: Creates canonical snapshot and SHA-256 digest in `.agents/canonical/AGENTS.md`.
   - `verify_canonical_contract`: Validates SHA-256 against canonical snapshot.
   - `restore_canonical_contract`: Restores root `AGENTS.md` from canonical snapshot or git HEAD.
   - `install_agents_pre_commit_guard`: Installs downstream git `pre-commit` hook to abort commits attempting to modify or decouple `AGENTS.md`.
2. `src/deploy_agents.sh`:
   - `_deploy_agents_symlink`: Creates cross-platform symlink pointing directly to `${REPO_DIR}/AGENTS.md`. On Windows, attempts native symlink (MSYS / mklink), falling back gracefully to NTFS hardlink with tip to enable Developer Mode for pure symlinks.
   - Automatically installs the pre-commit guard in target repositories.
   - Ensures root `AGENTS.md` is snapshotted and write-protected.
3. `bin/ainish-coder`:
   - Updated `--rules` and `--agents` to deploy `AGENTS.md` as a canonical symlink directly.
   - Added CLI management subcommands: `--lock-rules`, `--unlock-rules`, `--edit-rules`, `--restore-rules`, `--verify-rules`.
4. `bin/security_gate.py`:
   - Added `verify_canonical_agents_contract` check verifying `AGENTS.md` integrity against `.agents/canonical/AGENTS.md.sha256`.
5. `AGENTS.md` & `llms.txt`:
   - Added prominent top-of-file warning for all AI coding agents that `AGENTS.md` is an immutable symlink and edits must go to `llms.txt`.
   - Updated documentation boundary in `llms.txt`.
6. Verification & Deployment:
   - Automated test suite verified symlink creation, live propagation, write protection rejection, pre-commit hook blocking, and re-linking.
   - Deployed symlink to `d:/Code/local-router/AGENTS.md` and confirmed `samefile: True`.
   - Security gate passed with zero findings.
