# Task Record — AGENTS.md copy-deployment + guard-hook fix

- Date: 2026-09-19
- Branch: feat/agents-md-copy-deploy
- Status: complete
- Scope: src/deploy_agents.sh, src/agents_protection.sh, scripts/hooks/agents-md-sync.sh, scripts/setup-hooks.sh, AGENTS.md, llms.txt, src/help.sh, bin/ainish-coder, .agents/canonical/
- Triage: now
- Operator directive: stop symlink breakage — `ainish-coder --rules` must COPY the latest AGENTS.md; fix local-router hooks (failure emails)

## Objective
Replace the symlink + blocking-guard AGENTS.md deployment with plain tracked copies owned by each target repo, and migrate every deployed repo.

## Changes Completed
1. `src/deploy_agents.sh`: copy semantics (`_deploy_agents_copy`); rm-symlink-safe (never writes through links); idempotent `cmp` refresh; no root locking on deploy; global copies for ~/.agents + ~/.config.
2. `src/agents_protection.sh`: guard installer now REPLACES the legacy blocking AGENTS.md guard block in place (marker-spliced) with a warn-only symlink check; downstream commits touching AGENTS.md are allowed.
3. `scripts/hooks/agents-md-sync.sh`: post-merge/post-checkout re-syncs global COPIES (backup-then-copy), never symlinks.
4. AGENTS.md/llms.txt/help.sh/bin CLI: contract text moved from symlink model to copy-deployment model; `--verify-rules` now checks copy freshness; root is writable at the single source of truth.
5. Canonical snapshot refreshed (d8610c33…).
6. Migration: local-router, hermes-agent-backup-mac, wtf-is-going-on-mcp, ~/.agents, ~/.config, ~/AGENTS.md converted; legacy guard hooks swept across /mnt/d/Code (incl. posting, posting/vibes-posting) — zero blocking guards remain.
7. local-router: AGENTS.md migration committed (7666ae1) + merged (5e1b9eb); .gitleaksignore FP triage merged + pushed (3af1091) clearing daily Security Audit failures.

## Verification
- bash -n clean; security_gate.py PASS; 7/7 smoke tests (fresh deploy, legacy-symlink migration, commit-through-new-guard, idempotency, global copies, sync hook, root-untouched + zero symlinks).
- local-router: git diff --stat / git diff HEAD / git add -A / git status all clean-exit (previously broken by T-typechange + blocking hook); `--verify-rules` all green.
- gitleaks: no leaks found after FP triage (5 doc/test placeholders + 1 vendor-public installed-app OAuth constant per RFC 8252).
- CI test failures on origin were stale: antigravity-auth-platform-detect 15/15 and oauth-logout-suppression 2/2 pass locally on merged main.
