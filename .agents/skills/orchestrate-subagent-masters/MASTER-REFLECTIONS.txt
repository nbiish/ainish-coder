# MASTER-REFLECTIONS.txt — Continuous Action Reflection & Skill Refinement Ledger
#
# Protocol: Upon every dispatch of ANY subagent modality (dsh headless, dsh acp,
# native subagent, subagent_fork, workflow, ralph), the calling orchestrator MUST
# review subagent performance and record concise, actionable skill refinements
# below, synthesizing the 9 TTS.COMMS master directives.
# Format: ISO-8601 timestamp, subagent modality used, action summary, reflection & refinement.
# History note: pre-2026-09-05 entries record the legacy-engine era; engine names redacted
# per operator scrub directive — "legacy engine" denotes the retired pre-harness fleet.

================================================================================
GOVERNING TTS.COMMS MASTER DIRECTIVES (CROSS-DISPATCH SYNTHESIS)
================================================================================
1. Adversarial & Security Master:
   Strict loopback proxy confinement (127.0.0.1:11434). Use dummy bearer tokens
   (`local-router`). Subagents never receive or parse raw upstream API keys.

2. Privacy & Regulatory Master:
   Trajectory files and task descriptions in `/tmp`
   must be purged or scrubbed of sensitive environment details and absolute user paths.

3. Supply-Chain & Third-Party Risk Master:
   Pin upstream git commit hashes or release tags for engine sources in repository setup documentation.

4. Systems-Architecture & DevOps Master:
   Preflight probe on port 11434 (`curl -sf http://127.0.0.1:11434/api/version`)
   must pass prior to dispatching subagents. If down, auto-start via CLI shim.

5. Reliability & Verification Master:
   Enforce explicit step bounds (--max-steps 20-35). Require automated test suite
   execution and compilation checks on all generated unified diff patches before merge.

6. Governance & Sovereignty Master:
   Commit logs, task tracking files (`.agents/tasks/`), and comms ledgers
   (`.agents/comms/{date}-{time}-team.txt`) travel with git commits and maintain cryptographic auditability.

7. Developer Experience & Ergonomics Master:
   Standardize task file execution (`legacy engine run -f <file> --console-type simple`)
   over fragile shell string arguments to eliminate quoting errors across shells.

8. Agentic Orchestration & Multi-Model Master:
   Optimal chaining: dispatch Legacy engine first for structural AST codebase refactoring
   and patch creation, followed by Live-SWE-Agent for reproduction scripts and test hardening.

9. Performance & Resource Optimization Master:
   Discard failed subagent worktrees immediately (`git worktree remove --force`).
   Purge intermediate trajectory logs to prevent filesystem bloat.

================================================================================
ACTION REFLECTIONS & SKILL REFINEMENT ENTRIES
================================================================================

## 2026-09-02T16:58:30Z | Legacy terminal engines
- Context: Initial fleet unification under local-router/fallback-models shim.
- Observed Action: Subagents executed cleanly when routed to port 11434, but failed
  when port was occupied by standalone Ollama without backend proxying.
- TTS.COMMS Alignment: Addressed systems-architecture master (probe port 11434) and
  adversarial master (dummy token isolation).
- Skill Refinement:
  1. Mandate pre-dispatch curl probe to verify `local-router` readiness.
  2. Configure dual-stack auto-start (Local Router on 11434, real Ollama on 11435).
  3. Require `-f <task_file>` and `--console-type simple` for non-interactive the legacy engine.

## 2026-09-02T20:20:00Z | Cross-Platform Local-Router & Fleet Synchronization
- Context: Extending Ollama auto-start across macOS, Windows, Linux, and WSL.
- Observed Action: Windows environments lacked POSIX bash shim compatibility,
  preventing seamless terminal fleet routing.
- TTS.COMMS Alignment: Addressed developer-experience master (native Windows batch/PS1
  wrappers) and privacy/hygiene master (ephemeral worktree isolation).
- Skill Refinement:
  1. Generate `ollama.cmd` and `ollama.ps1` alongside POSIX `~/.local/bin/ollama`.
  2. Set `OLLAMA_HOST=127.0.0.1:11435` in GUI/User environments so desktop apps
     automatically yield port 11434 to Local Router.
  3. Ensure subagent task files and patches are systematically reviewed in isolated
     worktrees before operator merge confirmation.

## 2026-09-02T20:30:00Z | Free-Claude-Code, OhMyPy, and Legacy-Engine Fallback Cascade
- Context: Re-focusing headless execution chain (free-claude-code -> omp -> legacy engine),
  adding browser/CLI agent chat, agent selection, and legacy-engine fleet toggle.
- Observed Action: Users require flexibility to chat conversational dialogue directly,
  select specific agents, or trigger the autonomous fallback cascade while controlling
  subagent fleet expansion.
- TTS.COMMS Alignment: Addressed agentic-orchestration master (ordered cascade: FCC -> OMP -> legacy engine),
  developer-experience master (web UI + CLI command chat), and reliability master (fleet toggle guardrail).
- Skill Refinement:
  1. Formalize primary fallback cascade: `free-claude-code` -> `omp` -> the legacy engine.
  2. Implement agent selection (`auto`, `free-claude-code`, `omp`, the legacy engine, the legacy engine).
  3. Add legacy-engine fleet toggle: when enabled, subtasks can spawn autonomous fleet agents;
     when disabled, execution remains strictly within the top-level CLI session.
  4. Universally standardize on `local-router/fallback-models` via port 11434 for all agents,
     web chats, and configuration.


## 2026-09-05T01:09:27Z | legacy engine (TDD Reproduction Engineer) — Dispatch Protocol v3 live-fire
- Context: First v3 dispatch; scratch worktree om-v3-livefire; trivial green-probe test task.
- Observed Action: Liveness-only preflight passed (router /api/version up) but engine died at
  step 1 — router fallback target modal-proxy-glm-5.3-flash returned 401 upstream_http_auth
  ("Webhook token not found"); native exit 1, zero edits, scope clean, scrub clean.
  cwd-relative --output legacy engine_trajectory.json validated; non-interactive flags validated.
- TTS.COMMS Alignment: systems-architecture master (proxy liveness != upstream health),
  reliability master (fail-fast before steps are burned), adversarial master (dummy bearer only).
- Skill Refinement:
  1. Preflight MUST include a /v1/chat/completions round-trip probe (max_tokens 1), never
     /api/version alone.
  2. Engine exit 1 with auth/upstream signature normalizes to 60 ENVIRONMENT — never hand off
     to a sibling engine; all engines share the same router.
  3. Dispatch artifact paths are cwd-relative inside the worktree (validated live).

## 2026-09-05T01:45:50Z | dsh headless (DeepSeek Harness) — engine-swap live probe
- Context: Operator directive swaps fleet terminal engines (legacy engine/legacy engine) for DeepSeek Harness
  dsh (headless one-shot + ACP persistent surface); validation pong dispatched in worktree
  om-dsh-acp-swap before any doc swap was committed.
- Observed Action: `dsh --profile headless "Reply with the single word: pong..."` returned
  "pong" on stdout, reasoning on stderr, native exit 0 in ~8s — clean channel separation and
  inherently non-interactive. Works without the broken local-router (profile carries its own
  provider config), unblocking the fleet environment. Invoking directory = workspace root
  confirmed by README; launcher flags vs app flags cleanly split.
- TTS.COMMS Alignment: reliability master (real round-trip validation before doctrine swap),
  DX master (zero flags needed for one-shot), systems-architecture master (profile-owned
  provider config replaces single proxy assumption).
- Skill Refinement:
  1. Engine = dsh headless (one-shot) / dsh acp (persistent client surface); legacy engine/legacy engine
     retired to legacy-only on explicit operator request.
  2. Preflight = `command -v dsh` + `dsh --version` + pong round-trip (liveness is not health,
     re-confirmed on the new engine).
  3. Templates re-IDed persona-based (TPL_*_V3) — engine-agnostic names survive future swaps.

## 2026-09-07T21:40:00Z | dsh headless + orchestrator doctrine — v4 research distillation & local-router config directive
- Context: Operator-directed v4 rewrite: four research areas (DeepSeek Harness CLI/SDK docs,
  Anthropic ×4 publications, OpenAI ×5 publications, LongHorizon-Harness arXiv:2608.01964)
  distilled from the new RESEARCH-ANNOTATIONS.md (depth layer) into SKILL.md v4 (breadth layer);
  plus the 2026-09-07 directive pinning the fleet dsh agent config to the local-router ollama
  endpoint (agent-default-model: local-router/fallback-models).
- Observed Action: Config change surfaced two machine-level failures, fixed live and verified:
  (1) `.credentials.yaml` must be a STRICT FLAT `KEY: value` mapping — the nested legacy
  document failed the rc.8 parser; flattened to five keys (backups kept). (2) `reasoningEffort:
  max` on agent-default-model is unsupported by the openai-completions router adapter
  (UNSUPPORTED_REASONING_EFFORT boot failure; row removed). Verification: router
  /v1/chat/completions round-trip HTTP 200 (chain served glm-5.3-flash); dsh pong exit 0 /
  stdout `pong` / silent stderr; session log records `ollama-local-router` +
  `local-router/fallback-models` (session.jsonl.zstd; slug = absolute path with / → -, wrapped in --).
- TTS.COMMS Alignment: adversarial master (dummy bearer only, loopback-only probes),
  systems-architecture master (round-trip preflight; verify CLIs by RUNNING them), reliability
  master (fail-loud credentials, no silent fallback), governance master (provenance-carried
  annotations; receipts gain the served provider/model).
- Skill Refinement:
  1. SKILL.md v4 doctrine: scaffold-carries-intelligence; fan-out-for-read /
     single-thread-for-write; cost bands (~15× multi-agent vs ~4× single, ~80% variance =
     tokens, ~5× recovery, small-slice-first). New §2.0 topology/fan-out rules: pipeline /
     orchestrator-worker / swarm axis, effort bands, caps (≤6 concurrent, depth 1, 6–8
     reliability ceiling, timeout 1800), conformity mitigation (diversify scaffolds), ownership
     boundaries, arbiter pattern. Task contract gains OUTPUT FORMAT + STOP CONDITIONS on every
     TPL block (ids keep V3 names for cross-reference stability).
  2. New §2.8 loop engineering (LongHorizon distillation, dsh-only, fallback machinery stripped
     per operator): Manager/Executor/Auditor rounds; verified-state checkpointing (rejected
     result = evidence, not progress); fresh context per action; recovery from goal + last
     verified checkpoint; final reply from verified state alone; ~30 rounds, executor 1800 s /
     manager+auditor 600 s episodes; secrets env/CLI (task text visible in child argv).
  3. New §4.0 runtime contract: raw exits 0/1/130 (SIGTERM = 0), config layer precedence + dump
     provenance, credentials resolution order + strict flat schema, workspace-write default
     (operator's danger-full-access supersedes; worktree isolation is the containment),
     AGENTS/CLAUDE.md 65,536-byte auto-load, session-log ground truth. §4.1 preflight adds the
     loopback round-trip probe + run-the-binary CLI verification; model verification =
     settings.yaml + session log. 2026-09-05 zai note superseded as current truth by
     ollama-local-router / local-router/fallback-models (dashboard remains source of truth).
  4. §5 evaluator separation (skeptical fresh-context evaluator, live-artifact inspection,
     refine-vs-pivot; crashed dispatch = re-dispatch from the artifact); §6 receipts gain
     `served:<provider/model>`; §7 gains gates 8 (no self-approved quality) + 9 (discoveries
     become dispatches) + three pitfall rows (flat credentials schema, no fan-out-for-write,
     no identical auditor scaffolds).
